package com.parking.backend.Enum;

public enum ROLE {
    ROLE_ADMIN,
    ROLE_EMPLOYEE,
    ROLE_MEMBER,
    ROLE_USER
  /* 
  
   ROLE_ADMIN("ADMIN"), // Dueño o administrador del sistema
    ROLE_EMPLOYEE("EMPLEADO"), // Empleados que gestionan el sistema
    ROLE_MEMBER("MIEMBRO"), // Usuarios abonados que interactúan con el sistema
    ROLE_USER("USUARIO");
  private final String displayName;

    // Constructor privado para inicializar los valores de las instancias
    private ROLE(String displayName) {
        this.displayName = displayName;
    }

    // Método para obtener el nombre de visualización
    public String getDisplayName() {
        return displayName;
    }

    @Override
    public String toString() {
        return displayName;
    } */
}
